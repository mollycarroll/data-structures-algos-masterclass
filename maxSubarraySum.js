// establish max sum and temp sum variables
// if length of array is less than the sum number return null
// loop through arr up to num, adding arr at i to max sum
// set temp sum equal to max sum
// loop through up to num, less than arr.length
// temp sum = temp - arr at i plus num, plus arr at i
// max sum = max between temp and max
// return max

const maxSubarraySum = (arr, sum) => {
    
}

console.log(maxSubarraySum([100,200,300,400], 2));